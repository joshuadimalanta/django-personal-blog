--------------------------------------------------
startproject, startapp
- settings.py installed apps
- create urls.py, forms.py
--------------------------------------------------
create models
- import reverse (where to go after)
--------------------------------------------------
create forms
- widgets
--------------------------------------------------
static & templates
- static inside app folder
- css and js folder inside static folder
- templates inside app folder
- blog_app and registration inside app folder
- settings.py template_dir, static_root
--------------------------------------------------
- settings.py LOGIN_REDIRECT_URL = '/'
--------------------------------------------------
class based views
- from django.views.generic import (TemplateView,
                                    ListView,
                                    DetailView,
                                    CreateView,
                                    UpdateView,)
- from django.contrib.auth.mixins import LoginRequiredMixin
- from django.urls impor reverselazy
--------------------------------------------------
function based views
- from django.contrib.auth.decorators import login_required
--------------------------------------------------
comment function
- from django.shortcuts import render,get_object_or_404,redirect
- from django.utils import timezone
- @login_required
--------------------------------------------------
superuser authentication
- from django.contrib.auth import views
- url_patterns = url(r'accouts/login/$',views.login,name='login'),
  url(r'accouts/logout/$',views.login,name='logout',kwargs={'next_page':'/'}),
- templates/registration/login.html
--------------------------------------------------
templates, css, scripts for text editor
--------------------------------------------------
